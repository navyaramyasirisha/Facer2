<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.we_are_infinity.facer.UI.FRAGMENTS.ImageSelectionFragment">

    <ImageButton
        android:id="@+id/btnSearch"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:background="@drawable/ic_search3x"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/btnCamera"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:background="@drawable/ic_camera3x"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/btnGallery"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:background="@drawable/ic_image3x"
        android:visibility="gone"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/btnActivate"
        android:layout_centerInParent="true"
        android:background="@drawable/ic_3x"
        tools:rotation="45"
        android:layout_width="110dp"
        android:layout_height="110dp" />

</RelativeLayout>
